# Path to Google Service Account JSON key file (default: ./service-account.json)
# Create a service account in Google Cloud Console, enable Sheets API,
# share the spreadsheet with the service account email, then download the JSON key
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./service-account.json

# Sheet ID from the URL: docs.google.com/spreadsheets/d/THIS_PART/edit
GOOGLE_SHEET_ID=1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms

# Worksheet (tab) name
GOOGLE_SHEET_NAME=Sheet1

# Column range within the worksheet
GOOGLE_SHEET_RANGE=A:BF

# Deposit in USD (fallback for % calculation)
DEPOSIT=100000

# Path to column mapping JSON file (default: ./column-mapping.json)
# Customize this to map your spreadsheet's column headers to the app's internal fields
COLUMN_MAPPING_PATH=./column-mapping.json

# Path to timeline config JSON file (default: ./timeline-config.json)
# Defines which charts and metric cards appear in the Timeline view
TIMELINE_CONFIG_PATH=./timeline-config.json

# Set to "false" to hide the Timeline charts view (default: true)
NEXT_PUBLIC_ENABLE_TIMELINE=false

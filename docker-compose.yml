services:
  trading-calendar:
    build: .
    container_name: trading-calendar
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - stack.env
    environment:
      - NODE_ENV=production
    volumes:
      - /opt_files/credentials.json:/app/service-account.json:ro
